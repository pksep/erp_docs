import { defineConfig } from 'vitepress'

export default defineConfig({
  title: 'ERP Docs',
  description: 'Центр помощи ERP Docs',
  themeConfig: {
    nav: [
      { text: 'Документы', link: '/' },
    ]
  }
})
